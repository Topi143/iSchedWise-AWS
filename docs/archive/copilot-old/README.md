# Archived Copilot Configuration

This folder stores `.github` and `.github/.agent` assets archived during cleanup on 2026-03-19.

## Why archived

These files were removed from active paths to reduce noise and keep only skills/workflows needed for iSchedWise day-to-day work.

## What is here

- `WORKSPACE_REFACTORING_SUMMARY.md`
- `ARCHITECTURE.md`
- `ischedwise-dev.agent.md`
- `CLEANUP_SNAPSHOT_BEFORE.md`
- `agents/` archived specialist personas not needed in active flow
- `skills/` archived stack-specific skill packs not needed for Flask/MySQL/Jinja/Tailwind
- `workflows/` archived workflows not kept active (`create`, `preview`, `status`, `ui-ux-pro-max`)
- `ui-ux-pro-max/` archived shared assets

## Restore

To restore a file, move it back to its original path under `.github` or `.github/.agent`.
